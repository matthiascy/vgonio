use egui::plot::{LineStyle};

pub struct LinePlotter {
    is_square_view: bool,
    is_same_tick_spacing: bool,
    animate_time: f32,
    show_coordinates: bool,
    line_style: LineStyle,
}

pub struct Plotter {

}